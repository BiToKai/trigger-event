name: Notification PR Mergée

on:
  pull_request:
    types: [closed]

jobs:
  check_merge:
    runs-on: ubuntu-latest
    if: ${{ github.event.pull_request.merged == true }}
    steps:
      - name: Message de succès
        run: echo "La PR a été mergée avec succès !"